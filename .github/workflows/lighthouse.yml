name: Lighthouse CI

on:
  # Run after deployment workflow completes on main branch
  workflow_run:
    workflows: ['Deploy to GitHub Pages']
    types:
      - completed
    branches: ['main']

  # Run on pull requests
  pull_request:
    branches: ['main']

  # Allow manual trigger
  workflow_dispatch:

# Only run if the deployment was successful
jobs:
  lighthouse:
    # Only run on successful deployment, PR, or manual trigger
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' || github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Determine base path
        id: basepath
        # Match deploy.yml: custom-domain deploys (public/CNAME present)
        # serve from root with empty basePath; subpath-only deploys use
        # /<repo-name>. Lighthouse must audit the same URL structure the
        # production deploy uses or it'll measure 404 pages.
        run: |
          if [ -s "public/CNAME" ]; then
            echo "value=" >> "$GITHUB_OUTPUT"
          else
            echo "value=/${GITHUB_REPOSITORY#*/}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build site
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: ${{ steps.basepath.outputs.value }}

      - name: Stage site for Lighthouse
        id: stage
        # lighthouserc.json serves ./out at the domain root and audits
        # root-relative URLs. That matches a custom-domain deploy, but a
        # subpath build prefixes every asset URL with /<repo-name> — served
        # from the root, all CSS/JS would 404 and Lighthouse would audit an
        # unstyled skeleton page (bogus scores, e.g. tap-target failures from
        # default user-agent styles). Nest the export under the same subpath
        # the production Pages deploy uses and audit the subpath URLs instead.
        # The page list is derived from lighthouserc.json (single source of
        # truth) so it can't drift from the config.
        run: |
          base="${{ steps.basepath.outputs.value }}"
          if [ -n "$base" ]; then
            mkdir -p "_lhci-site${base}"
            cp -r out/. "_lhci-site${base}/"
            flags="--collect.staticDistDir=./_lhci-site"
            pages=$(jq -r '.ci.collect.url[] | sub("^http://localhost/"; "")' lighthouserc.json)
            for page in $pages; do
              flags="$flags --collect.url=http://localhost${base}/${page}"
            done
            echo "flags=$flags" >> "$GITHUB_OUTPUT"
          else
            echo "flags=" >> "$GITHUB_OUTPUT"
          fi

      - name: Run Lighthouse CI
        run: |
          npm install -g @lhci/cli
          lhci autorun ${{ steps.stage.outputs.flags }}
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

      - name: Display Lighthouse Results Summary
        if: always()
        run: |
          echo "📊 Lighthouse CI Results Summary"
          echo "=================================="

          if [ ! -d ".lighthouseci" ]; then
            echo "❌ No Lighthouse results directory found"
            echo "This step will not fail the workflow."
            exit 0
          fi

          # Find all JSON reports
          json_files=$(find .lighthouseci -name "lhr-*.json" | sort)

          if [ -z "$json_files" ]; then
            echo "❌ No Lighthouse JSON reports found"
            echo "This step will not fail the workflow."
            exit 0
          fi

          echo "Found $(echo "$json_files" | wc -l) report files"
          echo ""

          # Install jq if not available
          which jq > /dev/null || (sudo apt-get update && sudo apt-get install -y jq)

          # Process each unique URL
          urls=$(echo "$json_files" | xargs -I {} jq -r '.requestedUrl // .finalDisplayedUrl' {} | sort -u)

          for url in $urls; do
            page_name=$(echo "$url" | sed 's|http://localhost:[0-9]*/||' | sed 's|/index.html||' | sed 's|/$||')
            if [ -z "$page_name" ]; then
              page_name="Home"
            fi
            
            echo "📄 Page: $page_name"
            echo "   URL: $url"
            
            # Extract scores from all reports for this URL
            for report_file in $json_files; do
              # Check if this report matches the current URL
              report_url=$(jq -r '.requestedUrl // .finalDisplayedUrl' "$report_file" 2>/dev/null || echo "")
              if [ "$report_url" != "$url" ]; then
                continue
              fi
              if [ -f "$report_file" ]; then
                perf=$(jq -r '.categories.performance.score * 100 | round' "$report_file" 2>/dev/null || echo "N/A")
                acc=$(jq -r '.categories.accessibility.score * 100 | round' "$report_file" 2>/dev/null || echo "N/A")
                bp=$(jq -r '.categories["best-practices"].score * 100 | round' "$report_file" 2>/dev/null || echo "N/A")
                seo=$(jq -r '.categories.seo.score * 100 | round' "$report_file" 2>/dev/null || echo "N/A")
                
                echo "   Run: Performance: ${perf}% | Accessibility: ${acc}% | Best Practices: ${bp}% | SEO: ${seo}%"
              fi
            done
            echo ""
          done

          echo "✅ Lighthouse CI completed successfully"
          echo "📥 Full reports available in workflow artifacts"

      - name: Upload Lighthouse reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: lighthouse-reports
          path: .lighthouseci
          retention-days: 30

      - name: Comment PR with Lighthouse results
        # always() so the score breakdown still reaches the PR when an
        # error-level assertion (accessibility/SEO) fails the lhci step —
        # that's exactly when the author needs the per-page table.
        # Fork PRs get a read-only GITHUB_TOKEN, so createComment would 403
        # and fail the whole run — skip the step there (scores remain in the
        # workflow log and artifacts).
        if: always() && github.event_name == 'pull_request' && !github.event.pull_request.head.repo.fork
        uses: actions/github-script@v9
        with:
          script: |
            async function run() {
            const fs = require('fs');
            const path = require('path');

            // Read Lighthouse results
            const resultsDir = '.lighthouseci';
            if (!fs.existsSync(resultsDir)) {
              console.log('No Lighthouse results found');
              return;
            }

            // Find all JSON report files
            const files = fs.readdirSync(resultsDir).filter(f => f.endsWith('.json'));
            if (files.length === 0) {
              console.log('No Lighthouse JSON reports found');
              return;
            }

            // Parse all reports and organize by URL
            const reportsByUrl = {};
            for (const file of files) {
              const filePath = path.join(resultsDir, file);
              const report = JSON.parse(fs.readFileSync(filePath, 'utf8'));
              const url = report.requestedUrl || report.finalDisplayedUrl;
              
              if (!reportsByUrl[url]) {
                reportsByUrl[url] = [];
              }
              reportsByUrl[url].push(report);
            }

            // Helper function to get median score
            function getMedianScore(scores) {
              const sorted = [...scores].sort((a, b) => a - b);
              const mid = Math.floor(sorted.length / 2);
              return sorted.length % 2 === 0 
                ? (sorted[mid - 1] + sorted[mid]) / 2 
                : sorted[mid];
            }

            // Helper function to get score emoji and status
            function getScoreDisplay(score) {
              const percentage = Math.round(score * 100);
              if (percentage >= 90) return { emoji: '🟢', status: 'Good' };
              if (percentage >= 50) return { emoji: '🟡', status: 'Needs Improvement' };
              return { emoji: '🔴', status: 'Poor' };
            }

            // Read thresholds and assertion levels from lighthouserc.json —
            // the single source of truth — instead of a hand-synced copy
            // (a copy here drifted from the real config once already).
            const lhciConfig = JSON.parse(fs.readFileSync('lighthouserc.json', 'utf8'));
            const assertions = lhciConfig.ci?.assert?.assertions ?? {};
            const numberOfRuns = lhciConfig.ci?.collect?.numberOfRuns ?? 1;
            const categories = ['performance', 'accessibility', 'best-practices', 'seo'];
            const thresholds = {};
            const levels = {};
            for (const category of categories) {
              const assertion = assertions[`categories:${category}`];
              if (Array.isArray(assertion)) {
                levels[category] = assertion[0];
                thresholds[category] = assertion[1]?.minScore ?? 0;
              } else {
                levels[category] = 'warn';
                thresholds[category] = 0;
              }
            }

            // Create comment header
            let comment = '## 🔦 Lighthouse CI Results\n\n';
            comment += `**Pages Audited**: ${Object.keys(reportsByUrl).length} | `;
            comment += `**Runs Per Page**: ${numberOfRuns} (showing median scores)\n\n`;

            // Process each URL
            let allPassed = true;
            for (const [url, reports] of Object.entries(reportsByUrl)) {
              let urlPath, pageName;
              try {
                urlPath = new URL(url).pathname;
                pageName = urlPath === '/' || urlPath === '/index.html' 
                  ? 'Home' 
                  : urlPath.replace('/index.html', '').replace(/\//g, ' ').trim();
              } catch (error) {
                console.error(`Error processing URL ${url}:`, error);
                continue; // Skip this URL
              }
              
              comment += `### 📄 ${pageName}\n\n`;
              comment += '| Category | Score | Status | Threshold |\n';
              comment += '|----------|-------|--------|----------|\n';

              for (const category of categories) {
                const scores = reports.map(r => r.categories[category]?.score ?? 0);
                const medianScore = getMedianScore(scores);
                const percentage = Math.round(medianScore * 100);
                const display = getScoreDisplay(medianScore);
                const threshold = Math.round((thresholds[category] || 0) * 100);
                const passed = medianScore >= (thresholds[category] || 0);
                
                if (!passed) allPassed = false;
                
                const categoryName = category.split('-').map(w => 
                  w.charAt(0).toUpperCase() + w.slice(1)
                ).join(' ');
                
                const passIcon = passed ? '✅' : '⚠️';
                comment += `| ${display.emoji} ${categoryName} | **${percentage}%** | ${display.status} ${passIcon} | ≥${threshold}% |\n`;
              }
              
              comment += '\n';
            }

            // Add summary
            comment += '---\n\n';
            if (allPassed) {
              comment += '✅ **All categories meet or exceed the configured thresholds!**\n\n';
            } else {
              comment += '⚠️ **Some categories are below the configured thresholds.**\n\n';
              comment += 'Categories below threshold are marked with ⚠️. ';
              comment += 'Warn-level categories do not fail CI; error-level categories fail the Lighthouse workflow.\n\n';
            }

            // Add helpful links
            comment += '**Legend:**\n';
            comment += '- 🟢 **Good** (90-100%): Excellent performance\n';
            comment += '- 🟡 **Needs Improvement** (50-89%): Some issues to address\n';
            comment += '- 🔴 **Poor** (0-49%): Significant problems\n\n';
            comment += '**Thresholds** (from lighthouserc.json):\n';
            for (const category of categories) {
              const categoryName = category.split('-').map(w =>
                w.charAt(0).toUpperCase() + w.slice(1)
              ).join(' ');
              comment += `- ${categoryName}: ≥${Math.round(thresholds[category] * 100)}% (${levels[category]})\n`;
            }
            comment += '\n';
            comment += '📥 Download the detailed HTML reports from the [workflow artifacts](';
            comment += `${context.payload.repository.html_url}/actions/runs/${context.runId}) for in-depth analysis.\n\n`;
            comment += '📖 Learn more about [Lighthouse scoring](https://developer.chrome.com/docs/lighthouse/performance/performance-scoring/)';

            // Post comment on PR. Never fail the workflow over a comment:
            // a permissions hiccup (e.g. a repo-in-org token restriction, or
            // a fork PR that slipped past the step-level guard) must not
            // mask green Lighthouse results.
            try {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
              console.log('Lighthouse results comment posted successfully');
            } catch (error) {
              core.notice(
                `Could not post the Lighthouse results comment (${error.message}). ` +
                'Scores are still available in the step log above and the workflow artifacts.'
              );
            }
            }

            await run();
